# pScan

**A tool executes a TCP port scan on a list of hosts using `scan`.** 

---
### Usage
```
pScan [command]
```
Available Commands:
```

├── completion
├── docs
├── help
├── hosts
│   ├── add
│   ├── delete
│   └── list
└── scan
```